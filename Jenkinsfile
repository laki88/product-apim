pipeline {
  agent any
  stages {
    stage('test') {
      steps {
        echo 'testing stage started'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy started.'
      }
    }
  }
}